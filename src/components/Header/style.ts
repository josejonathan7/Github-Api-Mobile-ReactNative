import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-around",
        alignItems: "center",
        width: "100%"
    },
    input: {
        borderColor: "#CCC",
        borderStyle: "solid",
        borderWidth: 1,
        padding: 5,
        width: "60%",
        borderRadius: 8,
        color: "#FFF"
    }
})